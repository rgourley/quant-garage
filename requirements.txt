# Dev convenience install for running every skill locally.
# The canonical dep list lives in pyproject.toml.
#
# Users of the library should:
#   pip install quant-garage[all]
#
# Contributors reproducing every path (including flat-files S3 + live WebSocket):
requests>=2.31
numpy>=1.26
scipy>=1.11
pandas>=2.0
pyarrow>=15.0
boto3>=1.34
s3fs>=2024.6
websocket-client>=1.7
